# --- SERVER CONFIGURATION ---
server.port=${PORT:8080}

# --- DATABASE CONFIGURATION (PostgreSQL - Persistent) ---
spring.datasource.url=${DB_URL}
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# --- JPA SETTINGS ---
spring.jpa.open-in-view=true
# CRITICAL FIX: Forces schema creation on a blank database
spring.jpa.hibernate.ddl-auto=create 
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- SECURITY CONFIGURATION ---
application.security.jwt.secret-key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970

# --- LOGGING (NEW) ---
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.com.vrappy.backend=DEBUG
```This is the final, updated configuration file, designed to connect to your PostgreSQL database, use the environment variables, and **force Hibernate to create the tables** (which was the root cause of the previous failure).

**CRITICAL: This file removes all H2 settings.**

### **Updated `application.properties`**

```properties
# --- SERVER CONFIGURATION ---
server.port=${PORT:8080}

# --- DATABASE CONFIGURATION (PostgreSQL - Persistent Connection) ---
# Reads connection details from Render Environment Variables
spring.datasource.url=${DB_URL}
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# --- JPA SETTINGS ---
spring.jpa.open-in-view=true
# CRITICAL FIX: Setting DDL to 'create' forces Hibernate to generate the tables on the new, empty PostgreSQL database.
spring.jpa.hibernate.ddl-auto=create
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- SECURITY CONFIGURATION ---
application.security.jwt.secret-key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970

# --- LOGGING ---
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.com.vrappy.backend=DEBUG